
const Subject = ({ singelData }) => {
    // console.log(singelData);
    console.log(singelData);
    return (
        <div>
            <div>
            </div>
        </div>
    );
};

export default Subject;